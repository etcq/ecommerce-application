import styles from './bucket.module.scss';

export default function Bucket(): React.JSX.Element {
  return (
    <div className={styles.wrapper}>
      <h1 className={styles.title}>The bucket page will be here</h1>
    </div>
  );
}
