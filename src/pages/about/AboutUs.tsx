import styles from './about-us.module.scss';

export default function AboutPage(): React.JSX.Element {
  return (
    <div className={styles.wrapper}>
      <h1 className={styles.title}>The about us page will be here</h1>
    </div>
  );
}
